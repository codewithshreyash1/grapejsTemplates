# Templates Preview App

This app previews all JSX templates from the `templates/` folder.

## How to run

1. Install dependencies:
   ```
npm install
   ```
2. Start the dev server:
   ```
npm run start
   ```
3. Open your browser at [http://localhost:5173](http://localhost:5173)

All templates will be visible on a single page for easy UI review.
